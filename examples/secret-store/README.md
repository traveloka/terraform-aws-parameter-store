Secret Store
============

Configuration in this directory creates an AWS SSM Parameter to store secret.

Usage
=====

Note: Please change the key_id into a valid AWS KMS Key ID.

To run this example you need to execute:

```bash
$ terraform init
$ terraform plan
$ terraform apply
```
